#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import com.dlsc.fxmlkit.fxml.FxmlViewProvider;
#if (${RESOURCE_BUNDLE} && ${RESOURCE_BUNDLE} != "")
import java.util.ResourceBundle;
#end

public class ${NAME} extends FxmlViewProvider<${CONTROLLER}> {
#if (${RESOURCE_BUNDLE} && ${RESOURCE_BUNDLE} != "")
    public ${NAME}() {
        super(ResourceBundle.getBundle("${RESOURCE_BUNDLE}"));
    }
#end
}
